[package]
name = "contacthi-spacetimedb"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
