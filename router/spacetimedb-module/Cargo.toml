[package]
name = "contacthi-spacetimedb"
version = "0.1.0"
edition = "2021"
description = "ContactHI — CHI/1.0 router-node shared state (SpacetimeDB 2.x)"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = "=2.0.4"
log = "0.4"
